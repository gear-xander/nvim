# nvim

Simple Neovim config using `lazy.nvim`.

Requires `Neovim 0.11+`.

Includes:

- basic settings and keymaps
- file tree with `nvim-tree`
- LSP for Lua, Python, Bash, and Terraform
- Treesitter syntax highlighting
- format on save
- Git signs
- `tokyonight` theme
